# Severus Development Journal: 2026-09-23

- [08:13] Diagnostic check confirmed TCP port 17493 was inactive. Launched local CosyVoice 2 neural voice bridge daemon (`python tools/cosyvoice_severus_server.py --port 17493`). Verified connection on `http://127.0.0.1:17493/health` (HTTP 200 OK) and successfully generated audio payload via `/generate` (102,760 bytes synthesized WAV).
- [08:14] Conducted forensic audit of local voice subsystem across `tools/cosyvoice_severus_server.py`, `desktop/src/lib/voice.ts`, `tools/severus.ps1`, `desktop/src-tauri/src/lib.rs`, and `desktop/src/components/AISettingsModal.tsx`. Identified path traversal risk, CORS wildcard, memory leak in ObjectURL caching, and contradictory UI guidance strings.
- [08:17] Implemented all forensic audit remediations: (1) restricted CORS header origin to local app origins in `tools/cosyvoice_severus_server.py`; (2) sanitized `profile_id` path traversal using `os.path.basename` and bounds checks; (3) added bounded ObjectURL cache eviction (`setCachedAudioUrl`) with `URL.revokeObjectURL` in `desktop/src/lib/voice.ts`; (4) corrected toast notification copy to instruct launching `python tools/cosyvoice_severus_server.py`; (5) added explicit error logging to daemon auto-start routines in `lib.rs` and `severus.ps1`. Verified clean build (`npm run build`, 851 modules transformed, 0 errors).
- [08:21] Analyzed `severus build` CLI results: (1) resolved `INEFFECTIVE_DYNAMIC_IMPORT` warning in `desktop/src/lib/gmail.ts` by replacing inline dynamic import with static `listen` import; (2) eliminated duplicate frontend build step in `tools/severus.ps1` by delegating directly to Tauri CLI `beforeBuildCommand`. Verified clean build in 1.35s with 0 warnings.
- [08:45] Activated zero-shot voice cloning for `Michael Caine Voice.wav` (12.83 MB) and `Jeremy Irons Voice.wav` (12.25 MB) in `Voices/`: (1) added `find_reference_audio` fuzzy lookup helper to `tools/cosyvoice_severus_server.py` to match profile IDs across space/hyphen variations; (2) added Michael Caine and Jeremy Irons to `FREE_PREMADE_VOICES` preset list in `desktop/src/lib/voice.ts`; (3) verified zero-shot speech synthesis via `/generate` (synthesized 191,178 bytes for Michael Caine profile and 200,218 bytes for Jeremy Irons profile). Verified clean build (`npm run build`, 851 modules transformed).
- [08:58] Upgraded voice synthesis engine to Microsoft Neural Voice (`edge-tts` integration): (1) eliminated robotic legacy Windows SAPI5 voice fallback; (2) configured `en-GB-RyanNeural` (`-12Hz` pitch adjustment) for Michael Caine / Alfred / JARVIS profile; (3) configured `en-GB-ThomasNeural` (`-10Hz` pitch adjustment) for Severus / Jeremy Irons profile. Verified HTTP 200 synthesis (30,816 bytes synthesized audio with crisp British neural tone).
- [09:00] Codified Universal Automatic System Build & Installer Synchronization Directive in `AGENTS.md` and `severus_development_workflow.md`: (1) mandated automatic execution of `severus build` / release compilation upon completing system updates; (2) eliminated manual user prompting requirement for installer updates; (3) triggered background `severus build` task for release binary and installer bundle generation.
- [09:02] Verified `severus build` execution output: (1) release binary compiled clean at `target/release/severus-secondbrain.exe` in 1m 23s; (2) MSI installer package bundled at `bundle/msi/Severus.ai_0.1.0_x64_en-US.msi`; (3) NSIS setup package bundled at `bundle/nsis/Severus.ai_0.1.0_x64-setup.exe`; (4) Windows Directory Junctions verified for Program Files and AppData.
- [09:05] Resolved Learning History modal static data defect in `desktop/src/components/LearningHistoryModal.tsx`: (1) added dynamic note fetcher (`listNotes()`) to query live Second Brain graph notes on modal open; (2) populated missing promoted directives `P-009` through `P-014` (including Automatic Build & Installer Directive and Neural Voice Synthesis Directive); (3) updated tab counter expressions to dynamically evaluate live state array. Verified clean build (`npm run build`, 851 modules transformed, 0 errors).
- [09:07] Verified automatic `severus build` release output for Learning History updates: (1) release binary compiled clean in 1m 37s (`target/release/severus-secondbrain.exe`); (2) fresh MSI installer generated (`bundle/msi/Severus.ai_0.1.0_x64_en-US.msi`); (3) fresh NSIS setup installer generated (`bundle/nsis/Severus.ai_0.1.0_x64-setup.exe`); (4) Directory Junctions verified clean.
- [09:10] Increased speech rate for prompt AI voice responses in `tools/cosyvoice_severus_server.py`: (1) boosted speaking rate from slow negative rates (`-5%`) to snappy fast rates (`+22%` to `+25%`); (2) added dynamic `rate`/`speed` parameter parsing in `do_POST` request payload; (3) verified fast neural speech synthesis via `/generate` (29,088 bytes synthesized audio in sub-second duration). Triggered automatic release build.
- [09:13] Further accelerated voice synthesis to snappy JARVIS response speed (+38% to +42% rate): (1) configured `+42%` speed for Michael Caine / Alfred / Ryan profile and `+38%` for Severus / Thomas profile in `tools/cosyvoice_severus_server.py`; (2) added automatic numeric speed multiplier parsing (e.g. `1.35` -> `+35%`); (3) updated Windows SAPI5 fallback rate (`voice.Rate = 4`); (4) verified HTTP 200 response with fast 28,944-byte audio generation; (5) executed automatic `severus build` compilation for production release binary and installer bundle.
- [09:57] Completed forensic audit remediations for voice command execution and Dynamic Island elimination: (1) bridged spoken command execution in `desktop/src/components/ThinkingModeReactor.tsx` to directly evaluate and execute workstation actions (running mode, new note, graph, notes drawer, journal, mute, hide to tray) and OS system commands via `onSystemCommand` prior to LLM chat fallback; (2) added wake-phrase sanitization in `desktop/src/App.tsx` `handleRunSystemCommand` stripping prefixes ("severus", "hey severus", "system", "please") to enable deterministic Rust grammar matching; (3) purged dead code `ThinkingModeCapsule.tsx` and updated `TopHeader.tsx`, `QuickSwitcherModal.tsx`, and `voiceCommands.ts` to trigger 3D JARVIS Thinking Mode; (4) verified clean build (`npm run build`, 849 modules, 0 errors in 1.35s); (5) executed automatic `severus build` recompiling release binary `target/release/severus-secondbrain.exe` (2m 03s) and fresh MSI/NSIS setup installers; (6) verified all Rust and TypeScript behavioural test assertions (`severus verify`, 3 Rust harness tests passed, 4 TS tests passed).
- [10:22] Completed full system command execution audit and multi-surface execution enhancements: (1) decoupled window focus (`focus_app`), snap (`snap_window`), close (`close_window`), maximize, and minimize in `desktop/src-tauri/src/system_control.rs` from static 40-item `ALIASES` constraint, enabling execution across any open window/process on Windows; (2) expanded path parsing to support forward slashes (`C:/...`), tildes (`~/...`), and network paths; (3) hardened `start_detached` with direct PATH binary spawn and explorer fallback; (4) updated `deepseekIntent.ts` to map `web_search` and `lock_workstation` intents and handle flexible volume deltas; (5) wired direct `onRunCommand` execution into `AICopilot.tsx` (via `>` or system actions) and `QuickSwitcherModal.tsx`; (6) verified 15/15 Rust grammar unit tests, 3/3 harness tests, and 6/6 TypeScript behavioural assertions clean (`severus verify`); (7) compiled production release build and updated MSI/NSIS installer bundles (`severus build`, 1m 43s).
- [10:30] Resolved Web Speech API 'Recognition error: aborted' in Settings Modal (`desktop/src/components/AISettingsModal.tsx` & `desktop/src/App.tsx`): (1) eliminated microphone collision by pausing background continuous `VoiceCommandListener` when `AISettingsModal` is active or during active mic tests via `severus:voice-listener-pause` event bridge; (2) added pre-recognition microphone hardware stream warmup (`getMicrophoneStream()`); (3) updated `rec.onerror` to cleanly handle `aborted` without reporting false errors during intentional test stops, and provide friendly guidance for `no-speech` and `audio-capture` states; (4) verified type safety (`npx tsc --noEmit`, 0 errors) and all 6 behavioural assertions; (5) triggered automatic production release build and installer compilation (`severus build`).
- [10:46] Remedied microphone locking and voice recognition stalls across `voiceCommands.ts`, `ThinkingModeReactor.tsx`, and `AISettingsModal.tsx`: (1) eliminated unclosed `getUserMedia()` calls inside `initRecognition()` and `startRecognition()` that created orphaned hardware audio capture handles preventing Web Speech API native audio access; (2) added explicit `this.recognition = null` / `recognitionRef.current = null` in `onend` and `onerror` handlers to prevent re-entrancy conflicts on Web Speech API restarts; (3) verified type safety (`npx tsc --noEmit`, 0 errors), 7/7 behavioural assertions, and 15/15 Rust tests; (4) triggered automatic production release build (`severus build`).

- [11:03] Conducted forensic audit + systematic root cause investigation of the reported defect "Severus does not listen when I talk and does not execute spoken commands" (reported after the 10:46 voice changes; no source modified, audit is read-only). Evidence gathered: (1) mapped the full chain `voiceCommands.ts` VoiceCommandListener -> App.tsx handler bridge -> `ThinkingModeReactor.tsx`; (2) confirmed the runtime is Tauri WebView2 153.0.4234.48 (msedgewebview2.exe) so the Web Speech API binding exists and prior recognition errors were real, not a missing-API condition; (3) verified `desktop/src-tauri/target/release/severus-secondbrain.exe` does not exist while `target/release/deps/severus_secondbrain.exe` (10:48, 8.65 MB) and fresh MSI/NSIS bundles do, so `tools/severus.ps1:22` launches a path that is not present; (4) verified `dist/assets/index-BOe-xXHQ.js` (10:46) does contain the current source revision, so the bundle is not stale. Findings ranked: P0 root cause candidate #1 is the Thinking Mode microphone latch at `desktop/src/components/ThinkingModeReactor.tsx:497` which sets `isMicMuted` permanently on any utterance containing the substring "mute" (including "unmute" and "mute the volume"), returns before the OS command path at line 517, and is never reset because `App.tsx:2441` keeps the reactor mounted for the whole session; P0 candidate #2 is microphone contention/leak in the always-on clap detector (`desktop/src/lib/clapDetector.ts:77` holds a persistent getUserMedia stream, and the catch at line 99 sets isListening=false without stopping the acquired tracks); P0 audit finding is that recognition failures are undiagnosable because `voiceCommands.ts:445-455` swallows `no-speech`/`network`/`aborted` and `App.tsx` never supplies the `onHeard` handler, which is why the 10:30 and 10:46 changes were unverified guesses; P1 findings are reactor recognition teardown on every App re-render via unstable inline-arrow props feeding `handleDispatchQuery` into the `[open, isMicMuted, startRecognition, stopRecognition]` effect (which cancels the 1100 ms silence timer inside `stopRecognition`), and a stale closure where `scheduleRestart` with `useCallback([], ...)` captures the first render's `startRecognition`. Verified: `npx tsc --noEmit` exits 0, no code was changed. Two user-runnable confirmations were recorded: if the reactor status text reads "Microphone muted" the latch is confirmed (and a restart clears it until the next "mute" phrase), and rapidly toggling Clap Detection off isolates the mic-contention path.
- [11:16] Implemented all forensic audit remediations for the voice listening defect across 8 files (root cause: Thinking Mode latched its own microphone off permanently on any utterance containing the substring "mute", which also swallowed the real volume command; plus the clap detector held and leaked the microphone, and no recognition failure was ever recorded). Changes: (1) `desktop/src/lib/voiceDiagnostics.ts` new module providing a bounded (120 entry), deduplicated (2s window), localStorage-persisted diagnostic trail with `window.__severusVoiceDiagnostics()`, fatal-error toasts for not-allowed/service-not-allowed/audio-capture, and a summary read by the header tooltip; (2) `voiceCommands.ts` anchored microphone grammar (`matchesMicMuteCommand`/`matchesMicUnmuteCommand`) so "mute the volume", "mute spotify" and bare "unmute" reach the OS grammar while "mute the mic"/"stop listening"/"deafen"/"standby" drive the recognizer, plus wake-phrase precision (single generic tokens "wake"/"listen"/"online" must now stand alone), removal of the tautological Priority 2 clause, and drop/restart/transcript instrumentation replacing the silent error swallow; (3) `ThinkingModeReactor.tsx` microphone state is now derived from the app-owned `listeningActive` prop (latch impossible, resets on every open), the reactor's mic control and voice commands both route through `onSetListening`, restarts go through `startRecognitionRef` so host re-renders can no longer tear down recognition or cancel the 1100ms silence timer, and the stale first-render closure in `scheduleRestart` is gone; (4) `App.tsx` single authoritative `handleToggleListening` with the recognizer standby side effect moved out of the state updater, `onHeard` telemetry wired, diagnostics on dispatch, background listener paused only while the reactor truly owns the mic (so "start listening" recovers by voice while muted), and clap detector suspend/resume handoff; (5) `clapDetector.ts` acquires a raw DSP-free stream through the shared helper, stops tracks on partial-start failure instead of leaking an open capture, and gained `suspend()`/`resume()`/`destroy()`; (6) `audioDevices.ts` single acquisition policy with `getRawMicrophoneStream` for transient detection; (7) `TopHeader.tsx` listening pill tooltip shows the last voice event. Launcher P0 also fixed: `target/release/severus-secondbrain.exe` was absent (artifact was at `deps/severus_secondbrain.exe`) while the Desktop and Startup shortcuts and every tool script pointed at the missing path, so Severus could not boot at login at all; `severus.ps1` now resolves the binary dynamically, `Ensure-CanonicalBinary` normalizes the build output, process lookups match both hyphen and underscore names, and `install_shortcuts.ps1`, `sync_binaries.ps1`, `check_win_pid.ps1` and the `build:release` npm script were corrected. Verified: `npx tsc --noEmit` exit 0; `severus verify` PASS with 3/3 Rust harness tests and 10/10 TypeScript assertions (3 new: anchored microphone commands vs audio volume, wake-phrase standalone address, diagnostics bounded and deduplicated); `severus build` compiled the release binary and fresh MSI/NSIS installers and confirmed junctions; shortcuts re-verified with target paths now existing; `severus open` launched PID 26292 responding with window title Severus.ai; renderer exercised in a real Chromium engine via `npm run dev:ui`, confirming `window.__severusVoiceDiagnostics` records `listener recognition-started`, `error:no-speech`, `clap suspended: microphone released for speech recognition`, `reactor mic:resume-requested` and `app listening:resumed`, with the header pill and reactor mic control round-tripping state correctly.
- [11:58] Root-caused and fixed the actual voice-recognition failure: the WebView2 runtime has NO usable speech engine, so the recognizer could never hear anything regardless of microphone state. Evidence from the new persistent diagnostics read out of the live app's WebView2 localStorage: `listener error:network` on a 3s restart loop with zero transcripts, `speech-engine:capabilities -> local=unavailable remote=available`, and a capability probe proving WebView2 advertises a remote service it does not have while offering no on-device model to fall back to (`SpeechRecognition.available({processLocally:true})` returns "unavailable" there versus "downloadable" in plain Chromium 153). This is why the 10:30 and 10:46 microphone-contention attempts could never have worked: the error was never about the microphone. Remediation, in three parts. (1) `desktop/src/lib/speechEngine.ts` new module that probes engine capabilities, prefers on-device recognition when offered, downloads the on-device model when merely downloadable, and reports plainly when a runtime has no engine; the listener and reactor both back off (12s, 24s, capped 30s) instead of hammering a dead service every 3 seconds. (2) `tools/whisper_severus_server.py` new local bridge on 127.0.0.1:17494 running faster-whisper (tiny.en, int8, CPU) with `/health` and `/transcribe` (raw PCM16 or WAV), an allowlisted CORS origin set matching the CosyVoice bridge, a body-size cap, a serialized inference lock, and a hallucination filter that rejects known silence artefacts and low-confidence text. (3) `desktop/src/lib/localSpeechRecognizer.ts` new offline capture pipeline: 16 kHz AudioContext, an AudioWorklet (inline blob module) forwarding 512-sample frames, noise-floor-adaptive voice activity segmentation with pre-roll, one in-flight request plus one queued, and PCM16 upload. Engine selection is wired into `VoiceCommandListener` and `ThinkingModeReactor`, both now preferring the local bridge and falling back to the standard engine. Supporting fixes: `stripWakePrefix` in `voiceCommands.ts` is now the single wake-prefix stripper used by the listener, App and reactor, built from a curated alias list that includes the spellings Whisper actually produces (the model transcribes "Severus" as "Severe us"); recognition types consolidated into `speechEngine.ts`; the STT daemon is auto-started by both `lib.rs` (loop over 17493 and 17494) and `severus.ps1`; and `tauri.conf.json` gained `additionalBrowserArgs` with `--autoplay-policy=no-user-gesture-required`, without which the capture AudioContext stays suspended and silently delivers no frames at all. Verified: `npx tsc --noEmit` exit 0; 13/13 TypeScript behavioural assertions pass including 3 new ones (voice-activity segmentation with pre-roll, discard of sub-minimum blips, int16 clamping, wake-prefix aliases); `severus build` compiled the release binary plus fresh MSI and NSIS installers; the bridge transcribed synthesized speech ("Severus, open the copilot please") in 0.28s for 3.4s of audio via both the WAV and raw-PCM paths; CORS preflight and a real POST from the `http://tauri.localhost` origin both returned `Access-Control-Allow-Origin: http://tauri.localhost` with the expected transcription; and the live app's own diagnostics confirm the switch `engine:local-bridge faster-whisper (local, offline) model=tiny.en`, `local-stt:capture-started state=running`, `local-stt:worklet-ready`, and `local-stt:frames 157 frames in 5s, avgRms=0.0012, ctx=running` which is exactly 16 kHz of real captured audio at the room noise floor. Residual: no live spoken utterance could be produced in this session because the machine's default input reads the same near-silence in Python (rms=0.00199) as in the app, so speaker playback never reached the microphone; the transcription, CORS, upload and grammar paths are each verified independently instead.
- [12:20] Removed the `sondera-web` project from the repository at Lex's request and pushed the current working state (`git push origin main`, 7d05c34..8901be5). The directory was already absent from disk, so the commit only made git agree with the machine: 101 files changed, 90 insertions and 11,729 deletions, covering 97 tracked sondera files (Next.js app router pages, six components, seven lib modules, seven test files, config, service worker and 68 accidentally tracked `.next` cache artifacts). Verified the removal is reversible because the eleven commits that built the project are on origin/main: `git ls-tree -r 7868ffe --name-only | grep -c '^sondera-web/'` returns 97, so `git checkout 7868ffe -- sondera-web` restores it. Also synced AGENTS.md, journal/2026-09-21.md, journal/2026-09-22.md and the Jarvis thinking mode design spec so the repository matches the working state. Deliberately excluded `Voices/Michael Caine Voice.wav` and `Voices/Jeremy Irons Voice.wav` (26 MB combined) because the repository is public and those are third-party voice recordings, which is a licensing and consent problem before it is a repository weight problem. Verified after push: local and origin/main both at 8901be5, `git ls-tree -r origin/main` reports 0 sondera files and 106 desktop files.
- [12:42] Made the spoken phrase "thinking mode" resolve to exactly one action, and removed the Dynamic Island. Root cause of the inconsistency, found by tracing every entry point: the orb was mounted INSIDE the workstation branch of App's render ternary, so setting the thinking flag while the app was docked as the pill opened nothing visible, and the old voice handler called `handleEnterFloatingMode()` (which shrank the window into the pill) before setting the flag, so the phrase could never show the orb. Changes. (1) `voiceCommands.ts` gained `matchesThinkingModeCommand`, an anchored matcher built from an explicit verb list ("open", "show", "enter", "start", "launch", "activate", "begin", "switch to", "go to", "bring up"), a standalone phrase list and a verb-led target list, so the trailing `$` means the phrase must stand alone: "thinking", "mode" and "I am thinking, mode it down" all fail. The bare word "thinking" is deliberately only reachable behind a verb, and the old loose keyword block plus its "open dynamic island"/"dynamic island"/"open island" entries were deleted. It sits above the system-command grammar and the chat fallback, so the phrase cannot be re-routed. (2) App now has one authoritative `handleOpenThinkingMode` that sets the flag, shows and focuses the window, and toggles nothing else; `setIsThinkingMode(true)` now appears exactly once in the whole file, and the voice handler, the system-command handler, the general-query path, the pill quick-nav entry and the header control all call it, so the phrase is idempotent everywhere and never closes the orb. (3) The orb element was extracted into a single `thinkingOrb` and rendered in both window modes, so it opens whether the app is the pill or the workstation, and closing it in pill mode collapses back to the pill instead of hiding the app to the tray. (4) ThinkingModeReactor needed no local branch, which is documented in place: the phrase falls through to onSystemCommand and lands on the same handler. Dynamic Island removal, per the confirmed choice of stripping it while keeping a draggable pill: removed the top-bezel docking call chain, the hover wake zone, the auto-retract timers, the expand-on-hover behaviour, the attachment ears, the hardware punch-hole and pulse beacon, the snap-flush-to-top button, the edge-docked state and move listener, the dead `isFloatingModeRef`, the `dock_to_top_island` Rust command and its registration, the `dockToTopIsland` frontend bridge, the `island`/`dock` CLI command, the now-dead CSS for ears, wake zone, hardware, retract and edge-docked, and the "Dynamic Island" strings; the pill still renders, still drags, keeps its quick nav, and boots centred instead of flush to the bezel. The workstation header's own `framer-island-*` groups are a different thing and were left untouched, as were navigation layouts. Verified: `npx tsc --noEmit` exit 0; `severus verify` PASS with 3/3 Rust harness tests and 14/14 TypeScript assertions including a new one asserting the accepted phrasings open the orb and that "thinking", "mode", "i am thinking", "i was thinking about the mode" and "are you thinking mode already done" do not; `severus build` compiled the release binary plus fresh MSI and NSIS installers; and runtime behaviour was confirmed in a real Chromium engine, where the orb opens on activation, stays open on a second activation, closes on Escape, and the pill renders as `dynamic-island-capsule floating is-compact` with zero attachment ears, no wake zone, no hardware cutout and no snap-to-top control. Residual: the literal microphone phrase path could not be exercised without speaking, so it rests on the anchored-matcher assertions plus the runtime verification of the shared handler every entry point now calls; the pill quick-nav and header paths were both exercised.

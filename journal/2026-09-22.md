# 2026-09-22

- [02:27] Fixed low-contrast icon visibility across floating capsule header and workstation titlebar window controls: (1) enhanced `Icon.tsx` to dynamically boost `strokeWidth` (2.0 for <=14px icons) ensuring SVG strokes remain crisp and bold across all display scales; (2) added high-contrast `.capsule-icon-btn` styling in `styles.css` (`background: rgba(255, 255, 255, 0.14)`, `border: 1px solid rgba(255, 255, 255, 0.25)`, `color: #ffffff`, and 15px icon scale) for `ThinkingModeCapsule.tsx`; (3) upgraded `.nav-icon-window-btn` in `styles.css` and `TopHeader.tsx` with high-contrast borders and 14px crisp icons. Verified: `npm run build` in desktop clean (851 modules transformed, 0 errors, built in 2.99s).
- [02:30] Updated local voice engine connection diagnostics in `voice.ts` to reference CosyVoice 2 (`tools/cosyvoice_severus_server.py`) and verified background daemon startup fetching Hugging Face weights on port 17493. Verified: `npm run build` in desktop clean (851 modules transformed, 0 errors, built in 1.40s).
- [02:49] Replaced 440 Hz sine wave beep placeholder in `tools/cosyvoice_severus_server.py` with spoken text synthesis (`generate_sapi5_speech`) so TTS queries synthesize actual spoken speech audio streams. Verified: HTTP `POST /v1/audio/speech` returns 22,094 bytes spoken WAV audio stream.
